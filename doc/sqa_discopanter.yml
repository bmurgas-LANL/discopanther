directories:
    - ${ROOT_DIR}/test/tests
specs:
    - tests
dependencies:
    - framework
repos:
    default: https://github.com/bmurgas-LANL/discopanter (push)
reports: !include ${ROOT_DIR}/doc/sqa_reports.yml
